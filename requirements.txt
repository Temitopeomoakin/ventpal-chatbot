# Streamlit + HTTP
streamlit>=1.32.0
requests>=2.31.0

# LangChain - EXACT version to match your vector DB
langchain==0.1.20
langchain-community==0.0.38

# Vector DB - compatible with langchain 0.1.20
chromadb==0.4.24

# Embeddings + Transformers stack
sentence-transformers>=2.6.1,<3
transformers>=4.40.0,<5
huggingface-hub>=0.22.0
accelerate>=0.30.0,<1
safetensors>=0.4.3,<1

# SQLite shim for Chroma on some Linux hosts
pysqlite3-binary>=0.5.4,<0.6 ; platform_system != "Windows"

# Older protobuf to avoid segfaults
protobuf==3.20.3

# Nice-to-haves (safe)
orjson>=3.9.10

# Translation (if needed)
deep-translator>=1.11.4
