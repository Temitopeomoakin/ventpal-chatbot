# Streamlit + HTTP
streamlit>=1.32.0
requests>=2.31.0

# LangChain - UPDATED to match code imports
langchain>=0.1.20,<0.3.0
langchain-community>=0.2.0,<0.3.0
langchain-huggingface>=0.0.3,<0.1.0
langchain-chroma>=0.1.0,<0.2.0

# Vector DB - compatible version
chromadb>=0.4.0,<0.5.0

# Embeddings + Transformers stack
sentence-transformers>=2.6.1,<3
transformers>=4.55.0,<5
huggingface-hub>=0.22.0
accelerate>=1.0.0,<2
safetensors>=0.4.3,<1

# SQLite shim for Chroma on some Linux hosts
pysqlite3-binary>=0.5.4,<0.6 ; platform_system != "Windows"

# Older protobuf to avoid segfaults on some hosts
protobuf==3.20.3

# Nice-to-haves (safe)
orjson>=3.9.10

# Translation
deep-translator>=1.11.4
